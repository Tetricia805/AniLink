# Database (PostgreSQL)
DATABASE_URL=postgresql://anilink:anilink123@localhost:5432/anilink_db

# JWT
SECRET_KEY=dev-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30

# MinIO / S3 (file uploads)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=anilink-uploads
MINIO_USE_SSL=false

# App
DEBUG=true
FRONTEND_ORIGIN=http://localhost:5173

# Optional: production CORS (comma-separated). If unset and DEBUG=false, FRONTEND_ORIGIN is used.
# CORS_ORIGINS=https://app.anilink.com,https://www.anilink.com

# Password reset email (optional; if unset, dev logs reset link to console)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@anilink.com
# FRONTEND_URL=http://localhost:5173

# Google OAuth (optional; required for "Continue with Google")
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

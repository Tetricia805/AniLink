# Docker env: frontend uses same-origin /v1, Vite proxy forwards to backend:8000
# Used when running via docker-compose (env passed in compose, this file is reference)
VITE_API_BASE_URL=/v1
VITE_PROXY_TARGET=http://backend:8000
